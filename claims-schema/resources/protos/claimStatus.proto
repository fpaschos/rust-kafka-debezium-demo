syntax = "proto3";

import "google/protobuf/wrappers.proto";

package claims.schema;

// Enum for ClaimStatus
enum ClaimStatus {
  OPEN = 0;
  CLOSED = 1;
  CANCELLED = 2;
  UNDER_REVISION = 3;
  ADD_NEW = 4;
}

